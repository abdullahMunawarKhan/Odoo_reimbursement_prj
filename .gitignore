# Dependencies
node_modules/
backend/node_modules/

# Environment Variables
.env
backend/.env
config.env
*.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS specific files
.DS_Store
.DS_Store?
._*
Thumbe.db
ehthumbs.db
desktop.ini

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo
.project
.classpath
.settings/

# Build and Production
dist/
build/
out/
.next/

# Database / SQLite
*.sql
!db.sql # Unignore db.sql if it's a schema (but check if it contains actual data first)
# For now, let's keep it unignored if it's a schema, 
# but if it contains users or sensitive data it should be ignored.
# Looking at the file size 1826 bytes it's likely just a schema.

# Docker
.dockercache
